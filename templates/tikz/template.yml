name: tikz
description: Static diagram drawn with TikZ (kind figure), exported to SVG and PDF
kind: figure
engine: tikz
role: diagram
next_steps:
  - "Write bits/__BIT_ID__/brief.md (intent, essential relations) before drawing."
  - "Edit bits/__BIT_ID__/src/figure.tex (standalone TikZ picture)."
  - "Build: just build __BIT_ID__   (-> dist/figure.svg, dist/figure.pdf)"
  - "Then: just check __BIT_ID__; fill bit.yml (description, tags, subjects, topics, levels, provenance); status usable publishes /bits/__BIT_ID__/."
