name: web
description: General interactive React component (kind interactive, engine react)
kind: interactive
engine: react
role: explorable
next_steps:
  - "Write bits/__BIT_ID__/brief.md (intent, essential relations) before coding."
  - "Edit bits/__BIT_ID__/src/index.tsx (default export = the whole interactive)."
  - "Develop: just dev __BIT_ID__     (opens http://localhost:4321/bits/__BIT_ID__/)"
  - "Export:  just export __BIT_ID__  (standalone static site in dist/web/)"
  - "Then: just check __BIT_ID__; fill bit.yml (description, tags, subjects, topics, levels); status usable publishes /bits/__BIT_ID__/."
