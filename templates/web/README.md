# __BIT_TITLE__

<!-- What can be explored, which parameters, intended classroom use. -->

- Kind: interactive · Engine: React
- Source: `src/index.tsx` (+ `src/styles.css`)
- Standalone page: `/bits/__BIT_ID__` in the web runtime; export with `just export __BIT_ID__`

## Workflow

```bash
just dev __BIT_ID__      # live preview
just check __BIT_ID__
just export __BIT_ID__   # dist/web/index.html + assets
```

## Notes
